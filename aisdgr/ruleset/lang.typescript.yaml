# ==============================================================================
# lang.typescript.yaml
# Ruleset for TypeScript Coding Standards
# ==============================================================================

ruleset:
  id: lang.typescript

  # ---------------------------------------------------------------------------
  # Included Rules
  # ---------------------------------------------------------------------------
  includes:

    # ---- Pattern Rules (Cross-language) ----
    - rule_id: LANG-CC-ALL-P01
      version: ">=1.0.0"
    - rule_id: LANG-CC-ALL-P02
      version: ">=1.0.0"
    - rule_id: LANG-CC-ALL-P03
      version: ">=1.0.0"
    - rule_id: LANG-CC-ALL-P04
      version: ">=1.0.0"
    - rule_id: LANG-CC-ALL-P05
      version: ">=1.0.0"
    - rule_id: LANG-CC-ALL-P06
      version: ">=1.0.0"
    - rule_id: LANG-CC-ALL-P07
      version: ">=1.0.0"
    - rule_id: LANG-CC-ALL-P08
      version: ">=1.0.0"
    - rule_id: LANG-CC-ALL-P09
      version: ">=1.0.0"
    - rule_id: LANG-CC-ALL-P10
      version: ">=1.0.0"
    - rule_id: LANG-CC-ALL-P11
      version: ">=1.0.0"

    # ---- Concrete Rules (TypeScript) ----
    - rule_id: LANG-CC-TS-C01
      version: ">=1.0.0"
    - rule_id: LANG-CC-TS-C02
      version: ">=1.0.0"
    - rule_id: LANG-CC-TS-C03
      version: ">=1.0.0"

  # ---------------------------------------------------------------------------
  # Applicability
  # ---------------------------------------------------------------------------
  applicability:
    domain: [lang]
    language: [typescript]
    stage: [develop]

  # ---------------------------------------------------------------------------
  # Governance
  # ---------------------------------------------------------------------------
  governance:
    version: "1.0.0"
    status: draft
    owner: aisdgr

    intent:
      purpose:
        - language_quality_standard
      notes:
        - "This ruleset enforces TypeScript coding standards for AI-generated code."

    responsibilities:
      ai:
        - code_generator
      human:
        - code_reviewer
      system:
        - compliance_checker

    governance_impact:
      audit_scope:
        - typescript_generated_code
      reporting:
        - compliance_report
        - violation_report
      risk_category:
        - language_safety
        - quality_standard
